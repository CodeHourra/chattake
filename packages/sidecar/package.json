{
  "name": "@chattake/sidecar",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/chattake-sidecar",
    "test": "bun test"
  },
  "dependencies": {
    "@chattake/shared": "workspace:*",
    "openai": "^4"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11"
  }
}
